import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './mainContainer.html',

})
export class MainContainer {
  title = 'this container will hold every page in the project and system will load it first';
}
